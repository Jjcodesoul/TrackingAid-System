<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| API Routes
|--------------------------------------------------------------------------
|
| The application does not currently define API endpoints, but Laravel's
| application bootstrap expects this file to exist.
|
*/

Route::middleware('api')->group(function (): void {
	// API routes can be added here when needed.
});
